import { memo, ReactElement } from "react";

const Inputbox = memo(function Inputbox({ label, placeholder, onChange }: {
    label: string,
    placeholder: string,
    onChange: () => void
}): ReactElement {

    return <div>
       
    </div>
})

export default Inputbox;